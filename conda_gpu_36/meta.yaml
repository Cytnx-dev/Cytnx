package:
  name: cytnx_cuda_36
  version: "0.5.2a"

source:
  path: ../

build:
  noarch: generic
  number: 0


requirements:
  build:
    - python 3.6
    - cmake  
    - pybind11 
    - boost
    - mkl
    - mkl-include
    - intel-openmp
    - setuptools
    - cudatoolkit
    - graphviz
    - python-graphviz
    - {{ compiler("c")}}
    - {{ compiler("cxx")}}

  host:
    - python 3.6
    - boost
    - mkl
    - mkl-include
    - intel-openmp
    - setuptools
    - cudatoolkit
    - graphviz
    - python-graphviz
    - {{ compiler("c") }}
    - {{ compiler("cxx") }}

  run:
    - python 3.6
    - boost
    - mkl
    - mkl-include
    - intel-openmp
    - setuptools
    - cudatoolkit
    - python-graphviz
    - graphviz
    - python-graphviz
    - {{ compiler("c")}}
    - {{ compiler("cxx")}}

about:
