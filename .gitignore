# Python
__pycache__
.python-version

# CMake
CMakeUserPresets.json

# Visual Studio Code
.vscode/

# this project
build/
Testing/
.ccache/

# need to be cleaned up
.cache/
*.swp
*.swo
dev_test
*.e
*.o
*.so

# Personal AI/coding-agent notes & local config (per-user, not shared)
# Claude Code
CLAUDE.local.md
.claude/*
# ...but the symlink to the shared skills in .agents/skills/ is committed, so
# every contributor's Claude Code picks them up. No trailing slash: the pattern
# has to match a symlink, not a directory.
!.claude/skills
# OpenAI Codex
AGENTS.local.md
.codex/
# Gemini CLI
GEMINI.local.md
.gemini/
# Google Antigravity
.antigravity/
# Cursor
.cursor/
