#include "DenseUniTensor_test.h"
using namespace std;
using namespace cytnx;
TEST_F(DenseUniTensorTest, Init_by_Tensor){
    //  virtual void Init_by_Tensor(const Tensor &in, const bool &is_diag = false,
    //                             const cytnx_int64 &rowrank = -1);
    // EXPECT_NO_THROW(DenseUniTensor::Init_by_Tensor(utzero345, false, -1));
    // EXPECT_NO_THROW(DenseUniTensor::Init_by_Tensor(utzero345, true, -1));
    // EXPECT_NO_THROW(DenseUniTensor::Init_by_Tensor(utzero345, false, 0));
    // EXPECT_NO_THROW(DenseUniTensor::Init_by_Tensor(utzero345, false, 2));
}